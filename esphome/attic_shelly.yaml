---
substitutions:
  device_name: 'attic_shelly'

packages:
  common: !include 'packages/common.yaml'
  device: !include 'devices/shelly/1.yaml'

light:
  - platform: 'binary'
    output: 'relay1'
    id: 'output1'
    restore_mode: 'ALWAYS_ON'
    internal: true
