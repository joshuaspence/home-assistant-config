---
substitutions:
  device_name: 'laundry_shelly'

packages:
  common: !include 'packages/common.yaml'
  device: !include 'devices/shelly/25.yaml'

switch:
  - platform: 'output'
    output: 'output1'
    name: '${device_name} Output1'
    restore_mode: 'ALWAYS_ON'
    internal: true
  - platform: 'output'
    output: 'output2'
    name: '${device_name} Output2'
    restore_mode: 'ALWAYS_ON'
    internal: true
