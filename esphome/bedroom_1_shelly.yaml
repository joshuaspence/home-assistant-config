---
substitutions:
  device_name: 'bedroom_1_shelly'

packages:
  common: !include 'packages/common.yaml'
  device: !include 'devices/shelly/1L.yaml'

light:
  - platform: 'binary'
    output: 'relay1'
    id: 'output1'
    restore_mode: 'ALWAYS_ON'
    internal: true
