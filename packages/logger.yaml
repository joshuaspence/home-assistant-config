---
logger:
  default: 'warning'
  logs:
    # Temporary logging to debug "quota exceeded" warnings (see https://github.com/home-assistant/core/issues/44497).
    hass_nabucasa: 'debug'
    homeassistant.components.cloud: 'debug'
    homeassistant.components.cloud.google_config: 'debug'
    homeassistant.components.cloud.iot: 'debug'

    # Temporary logging to debug ADB timeouts.
    androidtv.adb_manager.adb_manager_async: 'debug'
    homeassistant.components.androidtv.media_player: 'debug'
    homeassistant.components.media_player: 'debug'

    # Reduce noise generated by `No response to 'Command.aps_data_indication' command` warnings.
    zigpy_deconz.api: 'error'
