---
recorder:
  commit_interval: 300
  exclude:
    domains:
      - 'sun'
      - 'weather'
    entities:
      - 'binary_sensor.workday'
      - 'sensor.bus'
      - 'sensor.snapshot_backup'
    entity_globs:
      - 'sensor.caltex_*'
  purge_keep_days: 30
